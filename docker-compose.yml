services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: qlp
      POSTGRES_PASSWORD: qlp_dev
      POSTGRES_DB: qlp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: qlp_minio
      MINIO_ROOT_PASSWORD: qlp_minio_dev
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

volumes:
  postgres_data:
  minio_data:
